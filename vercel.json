{
    "buildCommand": "cd frontend && npm run build",
    "outputDirectory": "frontend/build",
    "rewrites": [
      { "source": "/api/:path*", "destination": "https://flashback.inc:5000" },
      { "source": "/(.*)", "destination": "/index.html" }
    ]
  }